meta {
  name: POST - Error validación (precio negativo)
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/productos
  headers {
    Content-Type: application/json
    Authorization: Bearer {{adminToken}}
  }
  body {
    {
      "nombre": "Test",
      "descripcion": "Test",
      "precio": -100,
      "stock": 10,
      "categoriaId": 1
    }
  }
}

tests {
  assertStatus(response.status, 400);
}
