meta {
  name: Health Check
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/health
  headers {
    Content-Type: application/json
  }
}

tests {
  assertStatus(response.status, 200);
  assertContains(response.body, "OK");
}
