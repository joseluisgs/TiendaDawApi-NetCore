meta {
  name: [051] GET Mis Pedidos
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/pedidos/me
  headers {
    Authorization: Bearer {{userToken}}
  }
}

tests {
  assertStatus(response.status, 200);
  assertIsArray(response.body);
}
