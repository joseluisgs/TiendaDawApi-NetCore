meta {
  name: [014] GET By Id - No existe
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/categorias/999999
  headers {
    Content-Type: application/json
  }
}

tests {
  assertStatus(response.status, 404);
  assertPropertyExists(response.body, "message");
}
