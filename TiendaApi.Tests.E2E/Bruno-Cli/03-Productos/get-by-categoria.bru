meta {
  name: GET By Categoria
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/productos/categoria/1
  headers {
    Content-Type: application/json
  }
}

tests {
  assertStatus(response.status, 200);
  assertIsArray(response.body);
}
