meta {
  name: [059] ERROR - Rol USER
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/api/users
  body: none
  auth: bearer
}

auth:bearer {
  token: {{userToken}}
}

script:post-response {
  test("Users GetAll Forbidden - Status 403 Forbidden", function () {
      expect(res.getStatus()).to.equal(403);
  });
}

settings {
  encodeUrl: true
}

docs {
  Acceder a gestión de usuarios con rol USER debe retornar 403
}
