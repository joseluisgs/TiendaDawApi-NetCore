meta {
  name: DELETE Mi Cuenta
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/api/users/me/profile
  headers {
    Authorization: Bearer {{userToken}}
  }
}

tests {
  assertStatus(response.status, 204);
}
